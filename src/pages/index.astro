---
/**
 * ============================================================================
 *  Arquivo      : index.astro
 *  Projeto      : Site Pessoal Acadêmico — Prof. Haroldo
 *  Descrição    : Página inicial. Placeholder temporário do scaffolding,
 *                 agora envolvido no `BaseLayout` (plano 042) — confirma que
 *                 o layout base, cabeçalho e rodapé funcionam na rota `/`;
 *                 conteúdo real da Home (RF-20) chega no plano 044.
 *  Autor        : Desenvolvedor
 *  Criado em    : 2026-09-01
 *  Atualizado em: 2026-09-17
 *  Versão       : 0.2.0
 *
 *  Dependências : src/layouts/BaseLayout.astro
 *  Entradas     : nenhuma
 *  Saídas       : HTML estático da rota `/`
 *  Uso          : servido automaticamente pelo Astro como `src/pages/index.astro`
 *
 *  Notas        : conteúdo hardcoded é temporário — TODO(fase 3) abaixo.
 * ============================================================================
 */
import BaseLayout from '../layouts/BaseLayout.astro';

// TODO(fase 3): substituir pelo conteúdo real da Home (RF-20), vindo de
// content/perfil/index.md via o schema Zod (D-06), não hardcoded.
const professorName = 'Prof. Haroldo C. D. Lima Junior';
---

<BaseLayout title={undefined}>
  <h1 class="text-3xl font-semibold">{professorName}</h1>
</BaseLayout>
